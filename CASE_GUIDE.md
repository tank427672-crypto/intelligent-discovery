# 案例种子与展示指南

`case_seed/cases.json` 中的 20 条记录是展示和研究的候选清单，不是已验证的 `CaseRecord`。每条只保存原创的结构化一句话说明、公共来源 URL 与使用限制提示；不得复制原文、受版权保护的长摘录或完整文章。

## 导入门槛

将候选导入案例资产前，审核者必须建立：

`Source → Evidence → Finding → CaseRecord → Knowledge Graph`

并人工确认来源可访问、许可证/使用权限、引用要求、证据定位、案例结果与限制。无法确认时，保留候选或标注未知，不得声称“已验证”。

## 展示契约

案例卡片展示名称、类型、可信等级、来源数、证据数、更新时间和相关问题。详情必须有背景、问题、解决路径、结果、经验、失败因素、限制、证据链、图谱关系和反馈入口。候选在补齐证据前的可信等级为 `0`，验证状态为 `candidate`。
