# Identity + Local Network Intelligence Foundation（v0.7）

默认数据可见性为 private。用户可显式创建 shared/public 共享申请；本阶段只允许批准申请的记录，不做自动同步或自动发布。访问策略、导出/删除/撤销/访问请求均保留为独立可审计对象。

本阶段没有登录、第三方身份、组织协作、真实导出文件、实际删除执行或公共社区。它们必须在身份验证、撤销授权、数据保留、版权和审核角色得到架构决策后实现。
